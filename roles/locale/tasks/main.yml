---
- name: Generate locale {{locale}}
  locale_gen: name={{locale}} state=present

- name: Set locale to {{locale}}
  shell: |
    update-locale \
      LANG={{locale}} \
      LC_MESSAGES={{locale}} \
      LC_ALL={{locale}} \
      LC_COLLATE={{locale}} \
      LC_CTYPE={{locale}}
